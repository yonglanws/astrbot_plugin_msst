{
  "llm_provider_id": {
    "description": "用于生成剧本的 LLM 提供商",
    "type": "string",
    "hint": "选择 Astrbot 中已配置的对话模型提供商。留空则使用当前默认的提供商。",
    "default": "",
    "_special": "select_provider",
    "obvious_hint": true
  },
  "mss_api_url": {
    "description": "MySekaiStoryteller API 地址",
    "type": "string",
    "hint": "视频导出 API 服务器地址。同设备填 http://127.0.0.1:9881，跨设备填 http://<目标设备IP>:9881",
    "default": "http://127.0.0.1:9881",
    "obvious_hint": true
  },
  "export_timeout": {
    "description": "视频导出超时时间（秒）",
    "type": "int",
    "hint": "视频导出的最大等待时间，到达超时时间将自动取消导出。",
    "default": 600
  },
  "max_concurrent_exports": {
    "description": "最大并发导出数",
    "type": "int",
    "hint": "同时进行的视频导出任务数上限。增大此值会提高并发能力但可能增加服务器负载。",
    "default": 1
  },
  "temp_dir": {
    "description": "临时文件存储目录",
    "type": "string",
    "hint": "生成的视频、剧本等临时文件存储位置。留空使用插件数据目录。",
    "default": ""
  },
  "callback_api_base": {
    "description": "AstrBot 文件服务的外部可访问地址",
    "type": "string",
    "hint": "用于视频发送的回调地址，格式如 http://<外部IP>:<端口>。Docker 部署时填写宿主机IP和映射端口，如 http://192.168.31.54:6185。留空则自动检测。",
    "default": ""
  },
  "test_mode": {
    "description": "测试模式",
    "type": "bool",
    "hint": "启用后，「视频对话」和「剧本生成」功能将提示维护中，仅「测试视频对话」和「测试剧本生成」可正常使用。",
    "default": false
  },
  "personas": {
    "description": "角色人格列表",
    "type": "template_list",
    "hint": "每条绑定一个 Live2D 角色（与渲染宿主 models.yaml 的 name 或 shortName 一致）。绑定后，聊天和剧本生成都会使用该条目的整段人设提示词；未配置的角色由 AI 依据角色名合理演绎，不注入任何内置人设。TTS 音色仍在渲染宿主 config.yaml 的 tts.characters 中按角色名配置。",
    "templates": {
      "persona": {
        "name": "人格",
        "items": {
          "character_name": {
            "description": "绑定的角色名",
            "type": "string",
            "hint": "与渲染宿主 resources/models/models.yaml 中的 name（如 晓山瑞希）或 shortName（如 瑞希）一致",
            "obvious_hint": true
          },
          "prompt": {
            "description": "整段人设提示词",
            "type": "text",
            "hint": "该角色的完整人设：外貌、性格、说话风格、口头禅、禁忌、表情倾向等。将原文注入生成模板的角色段。"
          }
        }
      }
    },
    "default": []
  }
}